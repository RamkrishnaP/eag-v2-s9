prompt = f"""
You are a perception engine helping an AI agent to decide which MCP server(s) are most relevant to a user's query.

🔧 MCP Server Catalog:
{servers_text}

📜 Conversation History:
{history}

🧠 User Query:
"{user_input}"

🎯 Your Tasks:
- Identify the INTENT of the user's query.
- Extract important ENTITIES (e.g., company names, numbers, concepts).
- Resolve ambiguous references (e.g., "it", "that", "the X") using the 📜 Conversation History.
- If the user corrects a previous query (e.g., "I meant the X"), combine the new constraint with the previous topic (e.g., "Antigravity" + "IDE" -> "Antigravity IDE").
- Suggest a TOOL_HINT if you can guess a useful tool (optional).
- Pick the most relevant MCP server IDs from the catalog above.

📋 Your Output Format (in JSON):
{{
  "intent": "...",
  "entities": [...],
  "tool_hint": "...",
  "selected_servers": ["server_id_from_catalog"]
}}

✅ Rules:
- selected_servers must match server IDs exactly from the "MCP Server Catalog" list above.
- Do NOT use generic names like "server1" or "server2".
- If none are a clear match, select all available server IDs.
- Keep output short and clean.
- OUTPUT MUST BE VALID JSON ONLY. DO NOT INCLUDE ANY CONVERSATIONAL TEXT, MARKDOWN FORMATTING, OR CODE BLOCKS. JUST THE RAW JSON OBJECT.

Now generate the output:
"""